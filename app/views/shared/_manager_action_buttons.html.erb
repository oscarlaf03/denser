<div class="btn-group">
  <%= link_to "Editar", edit_product_path(product), class: 'btn btn-primary btn-md'%>
  <%= link_to "<i class='fa fa-trash-alt'></i> Eliminar".html_safe, product_path(product), method: :delete, data: {confirm: 'Una vez eliminado tu producto no podrá ser restaurado nunca'}, class: 'btn btn-danger btn-md'%>
</div>
